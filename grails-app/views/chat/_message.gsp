<div class="message message-${user.uuid }" style="color: #${user.color}">
	<span class="userName userName-${user.uuid }">
		${user.name }
	</span> : <span class="message">
		${message }
	</span>
</div>